#include <string>
#include <vector>

using namespace std;



int solution(string str1, string str2) {
    int answer = 0;

    for (int i = 0; i < str1.length(); ++i)
    {
        if (str1[i] == str2[i])
        {
            for (int j = 0; j < str2.length(); ++j)
            {
            }
        }
    }

    return answer;
}