#include <string>
#include <vector>

using namespace std;

int solution(string t, string p) {
    vector<int> t_Nums;
    
    // p 가 몇 자리 수 인지 분석한다.
    int pCount = p.length();
    
    // p 의 자릿 수 만큼 t 를 나눈다.
    for (int i = 0; i < t.size(); ++i)
    {
        
    }


    int answer = 0;
    return answer;
}